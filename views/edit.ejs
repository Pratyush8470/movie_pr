<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD db</title>

    <link rel="stylesheet" href="../style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="container mx-auto py-4">
        <form id="form" autocomplete="off" action="/updateData/<%= edit._id%>" method="post"
            enctype="multipart/form-data">
            <div class="w-1/2 wrap">
                <h3 class="title text-[30px] font-extrabold my-5">Edit Movie Details</h3>
                <div class="">
                    <label>Choose Poster <span class="text-red-700">*</span></label>
                    <input type="file" name="path" id="entry" value="<%= edit.path%>">
                    <label>Movie Name <span class="text-red-700">*</span></label>
                    <input type="text" name="movieName" id="entry" value="<%= edit.movieName%>" placeholder=" e.g. HDFC,SBI
                        ... ">
                    <label>Ticket Price <span class=" text-red-700">*</span></label>
                    <input type="number" name="price" id="entry" value="<%= edit.price%>" placeholder=" e.g. 20000/-,
                        5000000/- ... ">
                    <label>Seat No <span class=" text-red-700">*</span></label>
                    <input type="number" name="seatNo" id="entry" value="<%= edit.seatNo%>" placeholder=" e.g.
                        123xxxxx29... ">
                    <label>Seat Holder Name <span class=" text-red-700">*</span></label>
                    <input type="text" name="name" id="entry" value="<%= edit.path%>" placeholder=" e.g. jay,raj ... ">
                    <label>Mobile No <span class=" text-red-700">*</span></label>
                    <input type="number" name="mobileNo" id="entry" value="<%= edit.mobileNo%>" placeholder=" e.g. 9876543210
                        ... ">
                    <label>Payment Methods <span class=" text-red-700">*</span></label>
                    <input type="text" name="method" id="entry" value="<%= edit.method%>" placeholder=" e.g. card,cash ... ">
                    <label>Movie Details <span class=" text-red-700">*</span></label>
                    <textarea name="other" id="address" value="<%= edit.other%>" class=" px-[0.7rem] py-[0.3rem]"
                        placeholder="issues , check bounce ..."></textarea>

                    <button type="submit"
                        class="btn submit-btn block w-full rounded-lg py-2 mt-6 text-blck font-extrabold"> <span
                            class="add">ADD</span>
                        <span class="ml-1 plus"><i class="bi bi-plus-lg"></i></span></button>
                </div>
            </div>
        </form>

    </div>
</body>

</html>